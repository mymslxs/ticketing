import SigninForm from '../../components/auth.tsx/SigninForm';

const signin: React.FC = () => {
  return (
    <div className="container">
      <SigninForm />
    </div>
  );
};

export default signin;
